import { IResponse } from "src/app/core/models";

export interface CompanyRegisterResponse extends IResponse{
    id : number
  }


  export interface AnnouncementRegisterResponse extends IResponse{
    id : number
  }

  export interface CompensationRegisterResponse extends IResponse{
    id : number
  }